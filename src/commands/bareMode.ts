/**
 * baremode 结局文本
 *
 * 在这里添加你的结局内容，key 可以用于区分不同的结局触发。
 * 例如：root、stage0、secret 等。
 */
export const BARE_MODE_ENDINGS: Record<string, string[]> = {
  root: [
    "<br>",
    "The game has ended.",
    "<br>"
  ],
};
